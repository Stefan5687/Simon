# 🌙 Heute Abend: GitHub CLI installieren

## 1. GitHub CLI herunterladen

**Windows:**
```powershell
winget install GitHub.cli
```

**Mac:**
```bash
brew install gh
```

**Linux:**
```bash
sudo apt install gh
```

## 2. Einloggen

```bash
gh auth login --with-token
```

Dann deinen Token eingeben (von https://github.com/settings/tokens).

---

Token hast du schon erstellt! 🎉
